# 📁 Directory

**Directory** (called "Folder" on Windows) is a location in a filesystem that can contain files and other directories.

> [!NOTE]
> When a directory stored inside another directory, it is often called a _subdirectory_ or _child directory_ and, the outer directory called the _parent directory_.
