# Shell Program

**The Shell Program (`sh`, `bash`, `zsh`)** is a _compiled executable program_ containing raw machine code

Because it binary file, you can't read its internal code—If you try to view it using a command like `cat /usr/bin/sh` you won't see readable text, Instead you will see a screen full of garbage characters.
