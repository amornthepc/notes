# `whoami` command

`whoami` command is used to print the username of the current logged-in user to the terminal (`stdout`).

```bash
amornthepc:~$ whoami
amornthep

amornthepc:~$ sudo su
root:~# whoami
root
```
