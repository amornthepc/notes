# Root Directory

**Root Directory** (`/`) is the absolute top directory in the filesystem tree, it contains all other files and directories inside it.

A slash (`/`) represents the root directory **only** when it is the very first character (_the prefix_) of a filepath.

```bash
cd /
# Change current working directory to the root directory
```
