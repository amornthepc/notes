# `pwd` (_Print Woring Directory_)

`pwd` (_print working directory_) command is used to display the _absolute path_ of the current working directory (_the full location_).

```bash
pwd
# /home/amornthep
# /home/<username>
```
