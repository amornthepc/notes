# Machine Code

**Machine Code** is a set of instructions (binary `0`s and `1`s) that a computer understands and can **executes directly** without any help from other program.

> [!NOTE]
> **CPU** is the hardware that responsible for executing machine code.
